#include "Classes/Past.hpp"
#include <iostream>
/*
void Past::init()
{
    
}

void Past::handleEvent(sf::Event& event, sf::RenderWindow& window)
{
    // Delegar eventos a la lógica global o a la habitación activa
    // if (event.type == sf::Event::KeyPressed) { ... }
}

void Past::update(sf::Time dt)
{
    // Actualizar lógica del pathfinder, jugador y la habitación actual
    // pathfinder.update(dt);
}

void Past::render(sf::RenderWindow& window)
{
    // 1. Dibujar habitación actual (Necesitas saber cuál es la activa)
    // if (rooms.count("sala")) rooms["sala"].render(window);

    // 2. Dibujar jugador encima
    window.draw(spriteJugador);
}*/